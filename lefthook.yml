pre-commit:
  parallel: true
  commands:
    prettier:
      run: npx prettier --check .
    eslint:
      run: npx eslint .
    typecheck:
      run: npx astro check

pre-push:
  commands:
    test:
      run: npm run test:run
