// The Generator page has been removed.
// Prototypes are scaffolded via `npm run generate` (CLI) and populated by AI agents.
// See AGENTS.md → "Adding a Prototype" for instructions.
export default function Generator() {
  return null;
}
